# To create or update a token visit https://envio.dev/app/api-tokens
ENVIO_API_TOKEN="<YOUR-API-TOKEN>"

### DEVELOPMENT SETTINGS (FETCHED FROM SECRET MANAGER ON PROD) ###

# Per-chain RPC endpoints (set each to a valid RPC URL)
# Examples: https://mainnet.infura.io/v3/<PROJECT_ID> or https://rpc.ankr.com/eth
# Note: If these RPC variables are not present, the RPC fallback will simply not
# work — there is no need to configure anything else; the service will continue
# to run but without the per-chain fallback behavior.
ETHEREUM_MAINNET_ENDPOINT="https://mainnet.example.org/<API_KEY>"
GNOSIS_CHAIN_ENDPOINT="https://rpc.gnosischain.example.org/<API_KEY>"
BASE_MAINNET_ENDPOINT="https://base.example.org/<API_KEY>"
ARBITRUM_ONE_ENDPOINT="https://arbitrum.example.org/<API_KEY>"
APECHAIN_ENDPOINT="https://apechain.example.org/<API_KEY>" # If not present, ApeChain won't be indexed since its not supported by hypersync
LINEA_ENDPOINT="https://linea.example.org/<API_KEY>"
MANTLE_ENDPOINT="https://mantle.example.org/<API_KEY>"
POLYGON_MAINNET_ENDPOINT="https://polygon.example.org/<API_KEY>"
UNICHAIN_ENDPOINT="https://unichain.example.org/<API_KEY>"
CHILIZ_MAINNET_ENDPOINT="https://chiliz.example.org/<API_KEY>"
CELO_MAINNET_ENDPOINT="https://celo.example.org/<API_KEY>"
