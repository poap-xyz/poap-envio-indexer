{
  "name": "envio-docker-example-indexer",
  "version": "0.1.0",
  "scripts": {
    "clean": "rescript clean",
    "build": "rescript",
    "watch": "rescript -w",
    "mocha": "pnpm build && mocha",
    "codegen": "envio codegen",
    "dev": "pnpm build && envio dev",
    "start": "pnpm build && envio start",
    "test": "pnpm mocha"
  },
  "devDependencies": {
    "@rescript/react": "0.12.1",
    "rescript": "11.1.3",
    "mocha": "10.2.0"
  },
  "dependencies": {
    "envio": "2.24.0"
  },
  "optionalDependencies": {
    "generated": "./generated"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
